package handlers

import (
	"github.com/gofiber/fiber/v2"
)

func (h *Handlers) registerPostRoutes(c *fiber.App) {
	// r := c.Group("/posts")
	// r.Get("/", h.getPosts)
	// r.Post("/", h.createPost)
	// r.Get("/:id", h.getPostByID)
	// r.Delete("/:id", h.deletePostByID)
	// r.Put("/:id", h.updatePostByID)
}
